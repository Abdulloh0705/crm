# Base URL of the ZENIX CRM backend API.
# The frontend calls this with credentials:'include' so the backend must set an
# httpOnly session cookie (with appropriate CORS + SameSite/Secure config) rather
# than returning a token in the response body.
VITE_API_BASE_URL=http://localhost:4000/api
